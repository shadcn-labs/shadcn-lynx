import { Switch, SwitchThumb, SwitchTrack } from '@shadcn-lynx/ui/switch';

export default (
  <view style={{ padding: 24 }}>
    <Switch disabled>
      <SwitchTrack />
      <SwitchThumb />
    </Switch>
  </view>
);
