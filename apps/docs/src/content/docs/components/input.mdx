---
title: Input
description: Displays a form input field or a component that looks like an input field.
---

# Input

Displays a form input field or a component that looks like an input field.

## Installation

```bash
npx shadcn-lynx add input
```

## Usage

```typescript


export function App() {
  return (
    <view>
      <Input placeholder="Enter text..." />
    </view>
  )
}
```

import { ComponentPreview } from '@/components/component-preview'
import { InputDefaultDemo, InputDisabledDemo, InputTypesDemo } from '@/examples/input-demo'


## Examples

### Default

<ComponentPreview
  component={InputDefaultDemo}
  code={`<Input placeholder="Enter text..." />`}
/>

### Disabled

<ComponentPreview
  component={InputDisabledDemo}
  code={`<Input placeholder="Disabled input" disabled />`}
/>

### Types

<ComponentPreview
  component={InputTypesDemo}
  code={`<Input type="text" placeholder="Text input" />
<Input type="email" placeholder="Email input" />
<Input type="password" placeholder="Password input" />
<Input type="number" placeholder="Number input" />`}
/>

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| type | `text` \| `password` \| `email` \| `number` \| `tel` \| `digit` | `text` | Input type |
| placeholder | `string` | - | Placeholder text |
| disabled | `boolean` | `false` | Whether the input is disabled |
| value | `string` | - | Controlled value |
| defaultValue | `string` | - | Default value |
| onChange | `(value: string) => void` | - | Change handler |
| className | `string` | - | Additional CSS classes |
| ...props | `InputProps` | - | Native input props from @lynx-js/lynx-ui |
