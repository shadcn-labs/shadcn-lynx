---
title: Button
description: Displays a button or a component that looks like a button.
---

# Button

Displays a button or a component that looks like a button.

## Installation

```bash
npx shadcn-lynx add button
```

## Usage

```typescript


export function App() {
  return (
    <view>
      <Button>Click me</Button>
    </view>
  )
}
```

import { ComponentPreview } from '@/components/component-preview'
import { ButtonDefaultDemo, ButtonVariantsDemo, ButtonSizesDemo, ButtonDisabledDemo } from '@/examples/button-demo'

## Examples

### Default

<ComponentPreview
  component={ButtonDefaultDemo}
  code={`<Button>Default Button</Button>`}
/>

### Variants

<ComponentPreview
  component={ButtonVariantsDemo}
  code={`<Button variant="default">Default</Button>
<Button variant="destructive">Destructive</Button>
<Button variant="outline">Outline</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="link">Link</Button>`}
/>

### Sizes

<ComponentPreview
  component={ButtonSizesDemo}
  code={`<Button size="sm">Small</Button>
<Button size="default">Default</Button>
<Button size="lg">Large</Button>`}
/>

### Disabled

<ComponentPreview
  component={ButtonDisabledDemo}
  code={`<Button disabled>Disabled</Button>`}
/>

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| variant | `default` \| `destructive` \| `outline` \| `secondary` \| `ghost` \| `link` | `default` | The visual variant |
| size | `default` \| `sm` \| `lg` \| `icon` | `default` | The size variant |
| disabled | `boolean` | `false` | Whether the button is disabled |
| className | `string` | - | Additional CSS classes |
| onClick | `() => void` | - | Click handler |
| ...props | `ButtonProps` | - | Native button props from @lynx-js/lynx-ui |
