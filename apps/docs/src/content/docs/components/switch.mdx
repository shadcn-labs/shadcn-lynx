---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
---

# Switch

A control that allows the user to toggle between checked and not checked.

## Installation

```bash
npx shadcn-lynx add switch
```

## Usage

```typescript
import { Switch } 

export function App() {
  return (
    <view>
      <Switch />
    </view>
  )
}
```

import { ComponentPreview } from '@/components/component-preview'
import { SwitchDefaultDemo, SwitchCheckedDemo, SwitchDisabledDemo, SwitchWithLabelDemo } from '@/examples/switch-demo'

## Examples

### Default

<ComponentPreview
  component={SwitchDefaultDemo}
  code={`<Switch />`}
/>

### Checked

<ComponentPreview
  component={SwitchCheckedDemo}
  code={`<Switch checked />`}
/>

### Disabled

<ComponentPreview
  component={SwitchDisabledDemo}
  code={`<Switch disabled />`}
/>

### With Label

<ComponentPreview
  component={SwitchWithLabelDemo}
  code={`<view className="flex items-center gap-2">
  <Switch />
  <text className="text-sm font-medium">Airplane Mode</text>
</view>`}
/>

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| checked | `boolean` | - | The controlled checked state |
| defaultChecked | `boolean` | `false` | The default checked state |
| disabled | `boolean` | `false` | Whether the switch is disabled |
| onChange | `(checked: boolean) => void` | - | Event handler for checked changes |
| className | `string` | - | Additional CSS classes |
| ...props | `SwitchProps` | - | Native switch props from @lynx-js/lynx-ui |
