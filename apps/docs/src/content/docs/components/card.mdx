---
title: Card
description: Displays a card with header, content, and footer sections.
---

# Card

Displays a card with header, content, and footer sections.

## Installation

```bash
npx shadcn-lynx add card
```

## Usage

```typescript
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} 

export function App() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Card Title</CardTitle>
        <CardDescription>Card description</CardDescription>
      </CardHeader>
      <CardContent>
        <text>Card content goes here</text>
      </CardContent>
      <CardFooter>
        <text>Card footer</text>
      </CardFooter>
    </Card>
  )
}
```

import { ComponentPreview } from '@/components/component-preview'
import { CardDefaultDemo, CardWithButtonDemo } from '@/examples/card-demo'


## Examples

### Default

<ComponentPreview
  component={CardDefaultDemo}
  code={`<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card description goes here.</CardDescription>
  </CardHeader>
  <CardContent>
    <text>Card content goes here.</text>
  </CardContent>
  <CardFooter>
    <text>Card footer</text>
  </CardFooter>
</Card>`}
/>

### With Button

<ComponentPreview
  component={CardWithButtonDemo}
  code={`<Card>
  <CardHeader>
    <CardTitle>Create project</CardTitle>
    <CardDescription>Deploy your new project in one-click.</CardDescription>
  </CardHeader>
  <CardContent>
    <text>Project configuration options here.</text>
  </CardContent>
  <CardFooter>
    <Button variant="outline">Cancel</Button>
    <Button>Deploy</Button>
  </CardFooter>
</Card>`}
/>

## API Reference

### Card Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional CSS classes |
| ...props | `ViewProps` | - | Native view props |

### CardHeader Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional CSS classes |
| ...props | `ViewProps` | - | Native view props |

### CardTitle Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional CSS classes |
| ...props | `TextProps` | - | Native text props |

### CardDescription Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional CSS classes |
| ...props | `TextProps` | - | Native text props |

### CardContent Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional CSS classes |
| ...props | `ViewProps` | - | Native view props |

### CardFooter Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| className | `string` | - | Additional CSS classes |
| ...props | `ViewProps` | - | Native view props |
