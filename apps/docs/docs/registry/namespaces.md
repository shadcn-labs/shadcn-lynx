---
title: Namespaces
description: Registry namespaces and item types.
---

# Namespaces

Current item types include `registry:ui` and `registry:lib`.

Use `registry:ui` for components copied into `components/ui`. Use `registry:lib` for helpers copied into `lib`.

